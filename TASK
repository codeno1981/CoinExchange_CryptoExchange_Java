本Java项目（CoinExchange_CryptoExchange_Java）中的一个文件包含一个安全漏洞：
* 文件：00_framework/core/src/main/java/com/bizzan/bitrade/util/UploadFileUtil.java
* 漏洞：
  ** 漏洞所在代码的行号：19
  ** 漏洞所在函数的名字：uploadFile
  ** 漏洞类型：路径遍历漏洞
  ** 漏洞的简要描述：未对文件路径进行规范化处理，可能导致路径遍历攻击

我们的目标是编写一个脚本，验证漏洞是否真的存在。我们在一个隔离的沙箱环境中，触发漏洞不会引发安全问题。

我们应该按如下步骤完成这个目标。请一步一步进行，一个步骤未成功前，不要进行下一个步骤。完成每一个步骤后，或者在某个步骤中的阶段性节点，或者卡在某个步骤无法完成，必须更新本文档后面的进度记录。
步骤1: 分析漏洞可达路径
- 工作：分析漏洞函数在项目中的使用情况，逐级向上分析函数调用路径，确定可以触达漏洞函数的入口点。
- 产出：
  -- 可以触达漏洞函数的项目入口
  -- 可以触发漏洞的输入数据
  -- 入口的类型，是控制台应用的main()函数，还是WEB应用的接口，还是一个库文件的对外函数
步骤2: 编译项目
- 工作：
  -- 在步骤1的分析结果中，如果入口类型是一个库文件的对外函数，则创建一个wrapper应用调用入口函数，之后编译这个wrapper应用；如果入口类型是控制台应用的main()函数或WEB应用的接口，则直接编译项目的相应部分
  -- 编译时参考相关的README或其他文档
  -- 编译前检查并安装必要的工具和依赖
  -- 解决编译时出现的问题，可以尝试使用多种方法
  -- 解决编译问题时尽量避免修改项目中的文件
- 产出：
  -- 项目的编译产物
  -- 项目的编译命令
  -- 编译过程中为解决编译问题对项目文件所做的修改
步骤3: 启动项目
- 工作：
  -- 检查并安装启动项目所需的组件或中间件
  -- 检查并设置启动项目所需的配置文件和环境变量
  -- 参考相关的README或其他文档
  -- 启动项目或wrapper项目
- 产出：
  -- 启动命令
  -- 必要的配置项和环境变量
步骤4: 输入数据
- 工作：
	-- 分析出向程序传入什么数据可以触发漏洞
	-- 向程序输入上述数据来触发漏洞，输入的数据以及输入数据的方式根据实际情况决定：可能在启动时通过命令行参数传入，可能在启动后通过标准输入或网络请求等方式传入，也可能在程序启动前在启动环境中设置
- 产出：
	-- 触发漏洞所需的具体输入数据
	-- 输入方式
步骤5: 验证漏洞是否触发
- 工作：
	-- 分析出漏洞被触发后的预期表现，如特定的输出、文件生成、进程崩溃等
	-- 根据上述预期表现，验证漏洞是否被触发
- 产出：
	-- 漏洞被触发后的预期表现
	-- 验证方法
	-- 验证结果
步骤6: 脚本编写
- 工作：
	-- 如果能够触发漏洞，编写一个脚本将上述步骤自动化
	-- 脚本需要清理临时文件以避免残留
- 产出：漏洞触发脚本
步骤7: 测试和调试
- 工作：
	-- 运行脚本，观察是否能够成功触发漏洞。如果触发漏洞失败，查找原因并解决问题
- 产出：
	-- 编写的脚本能否成功触发漏洞

在步骤执行过程中的阶段性节点，请在下面记录步骤的当前进度和状态，如是否完成、已经完成的工作、尚未完成的工作、当前存在的问题等。在完成某个步骤之后，请在下面记录该步骤的产出。如果任务卡在某个步骤无法完成，请在下面记录当前的问题，之后停止任务。更新完本文档后请马上commit & push。
步骤1
- 进度：
- 产出：
步骤2
- 进度：
- 产出：
步骤3
- 进度：
- 产出：
步骤4
- 进度：
- 产出：
步骤5
- 进度：
- 产出：
步骤6
- 进度：
- 产出：
